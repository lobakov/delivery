/**
 * Пакет содержит классы, описывающие логику предметной области (доменную логику) приложения, т.е.
 * целевые операции, выполняемые над сущностями предметной области в рамках этой самой предметной области,
 * т.е. операции, имеющие смысл, независимо от IT-инфраструктуры.
 */

package com.github.lobakov.delivery.core.domainservices;
