/**
 * Пакет соответствует основному фундаментальному слою микросервисного приложения - Application Core,
 * в котором находится вся бизнес-логика приложения (доменная модель + доменная логика + юз кейсы + интерфейсы).
 */

package com.github.lobakov.delivery.core;
